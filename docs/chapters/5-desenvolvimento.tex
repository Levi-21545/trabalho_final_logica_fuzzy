\section{Desenvolvimento}

[DIRECIONAMENTO] Relatar a implementação prática: criação das funções de pertinência no scikit-fuzzy (crisp\_var\_dict, membership functions), definição das regras via ``skfuzzy.control.Rule``, construção do sistema de controle ``ControlSystem`` e ``ControlSystemSimulation``. Explicar como os dados do results.json são carregados e processados (leitura do JSON, extração de status\_code, response\_time\_ms, response\_size para cada execução). Mostrar fluxo: entrada crua → fuzzificação → inferência → desfuzzificação → score de anomalia. Incluir trechos de código relevantes (comentados em português) e um diagrama da arquitetura (diagrama-bloco do FIS acoplado ao fuzzer). Citar arquivos criados: main.py (script principal), fuzzy\_analyzer.py (módulo do FIS), sample\_results.json (dados de teste).
\endinput
